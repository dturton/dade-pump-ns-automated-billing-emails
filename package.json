{
  "name": "ar-invoice-sender",
  "version": "1.0.0",
  "private": true,
  "description": "NetSuite SDF project: daily Map/Reduce that emails open invoices (PDF) to customers",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf src/FileCabinet/SuiteScripts/ar-invoice-sender/*.js src/FileCabinet/SuiteScripts/ar-invoice-sender/lib",
    "test": "jest",
    "validate": "npm run build && suitecloud project:validate",
    "deploy": "suitecloud project:deploy"
  },
  "devDependencies": {
    "@hitc/netsuite-types": "^2026.2.1",
    "@oracle/suitecloud-unit-testing": "^1.9.0",
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
